# 胶片调整说明书 —— 每个控件做什么，怎么用由你定

[修图教程](EDITING_TUTORIAL.zh-CN.md) · [胶片教程](FILM_TUTORIAL.zh-CN.md) · [HDR 教程](HDR_TUTORIAL.zh-CN.md) · [使用说明](USER_GUIDE.zh-CN.md) · [架构与技术细节](ARCHITECTURE.zh-CN.md)

这份说明书和《修图工作流说明书》遵循同一个原则：只讲清胶片区每个滑条和下拉框**做的是什么**——它改变画面的哪一部分，你能看到什么变化。"这款胶片适不适合这张照片"，由你的眼睛决定。文中所有对比图都是 dngscan 按所列参数实际渲染的，所有数字都来自对这些渲染结果的直接测量。效果在屏幕上看不出来的组合，本文会直接说明，不放凑数的对比图。

先交代几个贯穿全文的词：

- **负片 / 反转片**：负片冲洗出来是底片，要再印到相纸（或电影拷贝）上才是成片；反转片（幻灯片）冲洗出来直接就是成片。
- **感色**：胶片对不同颜色的光敏感程度不同，这决定了它怎样区分颜色。
- **风格模式 / 冲印模式**：界面上"胶片模拟方式"的两个选项，"胶片风格 · AgX 成像"和"完整冲印 · 胶片全流程"，命令行里是 `observe` 和 `full`。前者只借用胶片的配色和明暗，后者把冲洗、印相整个流程都算一遍。

九张样张，覆盖胶片区的全部控件：

| 样张 | 相机 | 它演示什么 |
|---|---|---|
| 一 · 白松鼠公园 | Sony A7 V，ISO 1000 | 选胶片三联、感色强度四联、两种胶片模拟方式的差别 |
| 二 · 大逆光钢桥 | 富士 X100VI，ISO 250 | 白平衡固定色温 |
| 三 · 户外 coser 合照 | SIGMA fp，ISO 100 | 两处"效果确实很小"的例子（固定色温、肤色的区分） |
| 四 · 香港街景 | SIGMA fp，ISO 200 | AgX 色彩浓淡四联、胶片偏色（Kodachrome 的琥珀色阴影） |
| 九 · 园林庭院 | SIGMA fp，ISO 12800 | 胶片偏色（电影负片的绿青色阴影）——雕花木门是现成的深阴影 |
| 五 · 猫吧黑猫 | SIGMA fp，ISO 3200 | 钨丝灯下的白平衡、Portra 曲线预设——黑猫是现成的暗部 |
| 六 · live house 红光舞台 | SIGMA fp，ISO 25600 | AgX 色彩浓淡在极端饱和场景下的表现 |
| 七 · 清汤面碗 | SIGMA fp，ISO 12800 | 放大机色头 Y / M——青花白瓷是现成的中性参照物 |
| 八 · live house 品红后台 | SIGMA fp，ISO 12800 | Velvia 曲线预设——昏暗的场馆沉进反转片的黑里 |

---

## 第零课 · 思路：胶片是几层独立的设置，不是一键滤镜

选中一款胶片时，界面上有五个控件会**同时被设好**：白平衡（RAW 解码卡）、胶片感色和感色强度（颜色页）、曲线预设（成像卡）、AgX 色彩浓淡（成像卡）。它们全部看得见、改得了，没有哪一层藏在里面：设好的值就是你自己也能逐个去设的普通参数，你手动改过的项不会被覆盖。下一节的三联图就是这五层同时设好的样子；后面每一节拆开讲其中一层。

这几层分工如下：

- **白平衡**：这款胶片的标定色温（日光片 5500K，电影灯光片 3200K）。现场光的冷暖原样保留，不再被相机的自动白平衡抹平。
- **胶片感色**：这款胶片怎样**区分**颜色。肤色、植物各有各的区分方式，按厂商的数据手册建模；感色强度是这种效果的浓淡。
- **曲线预设**：这款胶片配上它的相纸或放映方式之后的明暗性格——黑有多黑、宽容度多大、高光怎样过渡。整卷固定，不随场景变。
- **胶片模拟方式**：以上设好之后，最终的颜色由谁来呈现。默认的风格模式里，胶片决定"看见什么"，颜色由 AgX 来成像，这是整个处理流程里验证得最充分的路径。冲印模式把成像也交给胶片，走"感光乳剂 → 特性曲线 → 印相 → 相纸 → 观看"的完整流程；灰阶校色、印相材料、印相曝光方式、成片调色、层间效应、胶片曝光、冲洗方式、高光预压缩、颗粒与光晕这些控件都属于这条流程（第六、七节）。

---

## 全部胶片选项一览

二十款胶片加五款影院变体，按负片和反转片分类。每款的标定色温、印相材料和风格搭配，都是界面上看得见、改得了的普通参数。"风格搭配"一列（感色强度倍率 × AgX 色彩浓淡）是**个人取向，不是测量结果**；其余各列来自数据手册。表中 base / punchy / muted 对应界面上的标准 / 浓郁 / 柔和。

**彩色负片 · 民用（11 款）**——印到各自配套的相纸上，可以用放大机色头：

| 选项 | 胶片 | 标定色温 | 印相相纸 | 风格搭配 |
|---|---|---|---|---|
| `portra160` | Kodak Portra 160 | 5500K 日光 | Portra Endura | ×1.3 · base |
| `portra400` | Kodak Portra 400 | 5500K 日光 | Portra Endura | ×1.3 · base |
| `portra800` | Kodak Portra 800 | 5500K 日光 | Portra Endura | ×1.3 · base |
| `portra800push1` | Portra 800 迫冲 +1 | 5500K 日光 | Portra Endura | ×1.35 · base |
| `portra800push2` | Portra 800 迫冲 +2 | 5500K 日光 | Portra Endura | ×1.4 · base |
| `ektar100` | Kodak Ektar 100 | 5500K 日光 | Portra Endura | ×1.4 · punchy |
| `gold200` | Kodak Gold 200 | 5500K 日光 | Portra Endura | ×1.4 · base |
| `ultramax400` | Kodak UltraMax 400 | 5500K 日光 | Portra Endura | ×1.4 · base |
| `superia400` | Fujifilm Superia X-TRA 400 | 5500K 日光 | Crystal Archive II | ×1.5 · base |
| `c200` | Fujifilm C200 | 5500K 日光 | Crystal Archive II | ×1.4 · base |
| `pro400h` | Fujifilm Pro 400H | 5500K 日光 | Crystal Archive II | ×1.3 · muted |

**彩色负片 · 电影（5 款）**——印到 Kodak 2383 放映拷贝上。每款另有一个"·影院放映外观"变体：保留影院暗厅里的高反差直接上屏，不按日常观看条件换算。合计五款影院变体。

| 选项 | 胶片 | 标定色温 | 印相材料 | 风格搭配 |
|---|---|---|---|---|
| `vision350d` | Kodak Vision3 50D | 5500K 日光 | Kodak 2383 | ×1.2 · muted |
| `vision3250d` | Kodak Vision3 250D | 5500K 日光 | Kodak 2383 | ×1.2 · muted |
| `vision3200t` | Kodak Vision3 200T | 3200K 钨丝 | Kodak 2383 | ×1.2 · muted |
| `vision3500t` | Kodak Vision3 500T | 3200K 钨丝 | Kodak 2383 | ×1.2 · muted |
| `verita200d` | Kodak Verita 200D | 5500K 日光 | Kodak 2383 | ×1.2 · muted |

**反转片（4 款）**——幻灯片冲出来就是成片：没有印相这一步，也没有放大机色头。它按暗室投影的条件标定（不含环境杂散光），导出时再换算到日常的明亮观看环境：

| 选项 | 胶片 | 标定色温 | 观看方式 | 风格搭配 |
|---|---|---|---|---|
| `provia100f` | Fujifilm Provia 100F | 5500K 日光 | 幻灯直看 | ×1.4 · base |
| `velvia100` | Fujifilm Velvia 100 | 5500K 日光 | 幻灯直看 | ×1.6 · punchy |
| `ektachrome100` | Kodak Ektachrome E100 | 5500K 日光 | 幻灯直看 | ×1.4 · base |
| `kodachrome64` | Kodak Kodachrome 64 | 5500K 日光 | 幻灯直看 | ×1.4 · muted |

全部选项都支持风格模式（默认）和冲印模式（第六节）；灰阶校色只在冲印模式下有效（第七节）。冲印模式下，上面 21 款负片（民用 11 + 电影 5 + 影院变体 5）都带印相数据，印相曝光方式的"随胶片曝光补偿"和"自定义"对它们全部可用；4 款反转片没有印相这一步，只能用固定。印相材料默认就是表里的搭配。另外准备了第二种材料的目前只有两组：Portra 400 另有 Kodak Supra Endura，Vision3 250D（含影院放映外观）另有 Kodak 2393 放映拷贝。只有这几款的印相材料下拉里才会出现第二项。

还有四项不分胶片型号、只属于冲印模式的调节，选中任何一款、切到冲印模式都会出现：冲洗方式（标准冲洗 / 自定义冲洗）、高光预压缩（连同压缩起点和高光褪色）、介质柔化（按介质数据 / 关闭）、颗粒种子。它们改的分别是负片怎么冲、光线进入胶片之前的场景高光、以及颗粒的随机排布，与上面的表无关，第六节逐个说明。

---

## 一 · 选一款胶片：选中时发生了什么

这个下拉框在界面上叫"胶片型号"。同一张公园样张，胶片分别选"无"、Portra 400、Velvia 100 各渲染一次，其余参数全部保持默认：

| 无 · 场景自适应 | Portra 400 | Velvia 100 |
|---|---|---|
| ![](assets/film-tutorial/park_none.jpg) | ![](assets/film-tutorial/park_portra400.jpg) | ![](assets/film-tutorial/park_velvia100.jpg) |

调了会发生什么（实测，2026-09-17 重新渲染）：切到 Portra 400 之后，大约一半的像素有看得出的变化（全图平均移动约 6.5/255）——暗部被托起来（负片相纸的黑比自适应的基准要浅），颜色变淡。切到 Velvia 100 之后，看得出变化的像素约三成七（平均约 4.8/255），亮度只略微上移，暗部更沉、颜色更浓。两款胶片动的是同一批控件，方向完全不同——这正是"一款胶片 = 五层设置的一组取值"的意思。

看什么：看树荫的绿。自适应的基准偏黄绿；Portra 把整体亮度抬起来，颜色放淡；Velvia 把绿压得更深、更密。再看画面底部的黄墙和人物，三张图里它们的亮度和色相各不相同。没有哪一层是"叠在成片上的滤镜"——整个处理流程看照片的方式都换掉了。

---

## 二 · 白平衡固定色温与镜前滤镜

原理在《使用说明》第四节讲过：固定色温不是用眼睛调白平衡，而是一个行业标准值，换算系数由照片文件自带的颜色标定精确算出。对胶片模拟来说，它的意义是**整卷统一**——胶片不会逐张适应现场光，5500K 的日光片在钨丝灯下就该偏橙。

傍晚的逆光钢桥，拍摄值对比 5500K 摄影日光：

| 拍摄值 · As Shot | 5500K · 日光片 |
|---|---|
| ![](assets/film-tutorial/bridge_default.jpg) | ![](assets/film-tutorial/bridge_wb5500k.jpg) |

调了会发生什么（实测，2026-08-28 按当时的处理流程重新渲染）：大约一半的像素有看得出的变化（平均移动约 5/255），画面整体变暖。相机的自动白平衡原本把傍晚的暖光"校正"掉了一部分，固定 5500K 之后这层自动适应没有了，现场光的暖原样保留下来。

一个反方向的对照：样张三的合照拍在正午日光下，fp 的拍摄值本来就在日光附近（整体只差约 0.1 EV）。同样切到 5500K，画面只有温和的整体变化（平均约 5/255，不到猫吧那张钨丝灯橙色变化幅度的四成），没有方向性的剧变。**拍摄值离标定色温越近，固定色温带来的变化越小。**这个选项去掉的是相机对场景的自动适应，不是往画面里加暖或加冷。

第三个方向是钨丝灯——"5500K 日光片在钨丝灯下就该偏橙"到底是什么样子。猫吧的黑猫拍在一整面暖黄灯带前，相机的自动白平衡把这份暖校正掉了大半；切到 5500K 日光片之后，酒墙整体沉进了钨丝灯的橙色里（实测约八成半的像素有看得出的变化，平均移动约 14/255，是本文所有白平衡对比里幅度最大的）：

| 拍摄值 · As Shot | 5500K · 日光片 |
|---|---|
| ![](assets/film-tutorial/cat_wb_asshot.jpg) | ![](assets/film-tutorial/cat_wb_5500k.jpg) |

拍过负片的人对这个方向不陌生：日光片带进钨丝灯的环境，不换卷、不加滤镜，冲出来就是这一片橙。想要这种"错得正确"的现场感，选日光片然后什么都别动；想校正它，用下面的镜前滤镜，按过去的做法来。

**镜前滤镜**（RAW 解码卡）是配套的独立选项：按柯达公布的滤镜参数，模拟装在镜头前的色温转换滤镜（85B 日光转钨丝、80A 钨丝转日光等）。它作用在胶片感色之前；HDR 的亮度测量也是透过滤镜进行的——真胶片就是这样测光的。滤镜没有强度滑杆，玻璃没有半片的。它用来复刻"灯光片加 85B 拍日光"这类传统做法，详见《使用说明》第五节。

---

## 三 · 感色强度：颜色区分效果的浓淡

> **现状说明（2026-09-17）**：生成胶片感色数据的工具此前有一个缺陷，它算出的相机色彩模型误差很大，胶片感色的效果因此被夸大了许多。修正之后，各款胶片感色的实际效果只有原来的几分之一：在公园样张上，Velvia 100 的感色强度从 ×0 拉到 ×2.5，已经没有任何像素的变化达到看得出的程度（平均只差约 1/255）。本节下面的四联图和数字是修正之前渲染的，保留作为"这个控件往哪个方向起作用"的示意，**现在的版本里它的效果要小得多**。是否在生成数据时把这个效果按比例放大，还没有定；定下来之后本节会整体重做。

先说原理：感色强度不是饱和度。胶片感色是按厂商数据手册建模的**颜色区分方式**（肤色、植物、青色、品红色各自往哪个方向分开），强度是这整套方式的倍率。1.0 是按数据手册校准的强度。每款胶片带一个推荐值（Velvia ×1.6，Portra 系 ×1.3，电影负片 ×1.2），推荐值是个人口味，不是测量结果。

Velvia 100 在公园样张上的强度对比，其余各层保持预设值（树荫的裁切）：

| ×0（关闭颜色区分） | ×1.0（校准强度） |
|---|---|
| ![](assets/film-tutorial/crop_strength_x00.jpg) | ![](assets/film-tutorial/crop_strength_x10.jpg) |

| ×1.6（预设推荐值） | ×2.5（夸张） |
|---|---|
| ![](assets/film-tutorial/crop_strength_x16.jpg) | ![](assets/film-tutorial/crop_strength_x25.jpg) |

调了会发生什么（实测）：×0 关掉颜色区分之后，大约一半的像素和推荐值相比有看得出的差别。（这张图按幻灯片本身的黑来渲染，裁切区的暗部层次比较多，有差别的面积也随之变大。）看什么：看前景的叶簇和背景的树影是怎样**分开**的。×0 的绿是均匀的一片黄绿；强度往上推之后，受光的叶子保持黄绿，背光的叶子被推向青蓝，同一丛树里的色相差距被拉开了。这就是"颜色区分效果的浓淡"，不是全图一起加饱和。

一个需要说明的边界：本文原本打算在合照样张上演示负片对肤色的区分，实测 Portra 400 的感色强度从 ×0 拉到 ×1.3，**在屏幕上看不出区别**（变化达到可见程度的像素只有 0.3%）。负片对肤色的区分本来就以温和为目标，这张合照又恰好落在它几乎不变的区间里。按本文的原则，看不见的对比不放图；所以改用颜色区分最强的 Velvia 加植物场景来演示同一个控件。你在自己的人像上把这个滑条拉到头、画面几乎不动时，滑条没有坏——这款胶片在这类内容上本来就变化很小。

---

## 四 · 曲线预设：整卷固定的明暗性格

选中曲线预设之后，一组固定的明暗参数会整体替换掉场景自适应。以 Velvia 100 为例：黑点在 −4.05 EV，反差、暗部、高光的形状都按厂商数据手册上的特性曲线固定下来，白点算出来在 +7.59。（旧版本里它的白点顶在拟合范围的上限 +8.5，那是把观看环境的杂散光算进了目标造成的；现在按幻灯片本身的黑来标定，同一组曲线就能容纳全部反转片，拟合误差 0.027 档。）场景分析照常运行，HDR 能亮多少仍然读真实场景；但明暗曲线不再看这张照片的直方图——**整卷一致，正是选它的目的**。

两款曲线，各自放到性格合适的题材上。只动曲线这一层，白平衡和胶片感色保持默认。

先看负片。正午的香港山道，画面从白墙、玻璃楼一路铺到绿山和晴空，全是明亮的中间调，正适合看清曲线在**看得见的亮度**上做了什么。自适应把白点算在 +4.16：白墙顶得透亮，整张照片是数码相机那种爽利。Portra 400 把白点定在 +6.13、反差 3.23，同样的场景整体沉下来一档：白墙有了灰阶，绿山变得厚重，天空的蓝往深里走。这就是负片印相的**中间调密度**：胶片不把"亮"用满，换来的是从白到黑每一档都留着颜色（约七成半的像素有看得出的变化）：

| 场景自适应 · 默认 | Portra 400 曲线预设 |
|---|---|
| ![](assets/film-tutorial/hk_curve_adaptive.jpg) | ![](assets/film-tutorial/hk_curve_portra400.jpg) |

特写看白楼和绿树。自适应下，校舍的白墙贴着参考白，细节全靠边缘撑着；Portra 把它压回中间调，墙面上每一层窗洞都分出了灰阶，树冠的绿从"发亮的绿"变成"有分量的绿"。把白墙拍成有灰阶的白，正是负片扫描件最容易认出来的样子：

| 自适应 · 白楼特写 | Portra 400 · 白楼特写 |
|---|---|
| ![](assets/film-tutorial/crop_hk_curve_adaptive.jpg) | ![](assets/film-tutorial/crop_hk_curve_portra400.jpg) |

再看反转片。Velvia 100 的黑点定在 −4.05，反差更硬——**反转片黑得干脆**：幻灯片最深的地方只透过约 0.5% 的光，这就是它黑得下去的本钱。live house 的后台视角，自适应把黑点放到 −8.3 EV，天花板的桁架、墙上的杂物都留在微光里；Velvia 的曲线把这整个上半场馆干脆利落地沉进黑里，品红色的灯板变浓，白衬衫白裤把乐手从黑里衬出来（近九成的像素有看得出的变化）：

| 场景自适应 · 默认 | Velvia 100 曲线预设 |
|---|---|
| ![](assets/film-tutorial/backstage_curve_adaptive.jpg) | ![](assets/film-tutorial/backstage_curve_velvia100.jpg) |

特写看顶棚。自适应让顶棚浮在一层棕绿色的微光里——夜店的天花板被数码相机翻了出来；Velvia 把它整个沉进黑里，灯的品红变浓，白衬衫从黑里跳出来。反转片的看法是：**黑就黑得干脆，亮的东西才有资格发光。**反转片直接投影，没有印相这一步替它做折中，这份决绝就是幻灯片观感的核心：

| 自适应 · 顶棚特写 | Velvia 100 · 顶棚特写 |
|---|---|
| ![](assets/film-tutorial/crop_backstage_curve_adaptive.jpg) | ![](assets/film-tutorial/crop_backstage_curve_velvia100.jpg) |

### 整卷一致：胶片不看直方图

只看一对样张，曲线预设像是"另一种精修"；要把两个场景放在一起，才看得出它实际是什么。自适应给每张照片单独算一组黑白点：昏暗的猫吧，它把黑点挖到 −6.3，白点只给 +4.13；晴天的博物馆正殿，它把黑点收在 −4.7，白点 +4.10——每一张都重新决定。Portra 400 给谁都是同一条曲线：黑 −4.89，白 +6.13。于是同一款胶片在猫吧主要动的是**黑的一端**（暗部托起来，黑猫的毛有了层次，黑仍然沉得住），在正殿主要动的是**白的一端**（白点上移两档，石殿和绿树沉进负片的中间调密度里）——两个场景被同一条曲线从不同方向拉了过来：

| 猫吧（暗场景）· 自适应 −6.3/+4.13 | 猫吧 · Portra 曲线 −4.89/+6.13 |
|---|---|
| ![](assets/film-tutorial/cat_curve_adaptive.jpg) | ![](assets/film-tutorial/cat_curve_portra400.jpg) |

| 博物馆正殿（亮场景）· 自适应 −4.7/+4.10 | 正殿 · Portra 曲线 −4.89/+6.13 |
|---|---|
| ![](assets/film-tutorial/temple_curve_adaptive.jpg) | ![](assets/film-tutorial/temple_curve_portra400.jpg) |

猫吧和正殿各有约六成半的像素出现看得出的变化。合在一起才是重点：两个毫不相干的场景用了同一条曲线，相邻两张的明暗性格连贯了起来。扫描仪吐出一整卷胶片时就是这种观感，这也是"选一款胶片"和"逐张精修"的全部区别。

两款的方向相反：负片既往上托也往下压，反转片一律干脆。**这就是"整卷固定的明暗性格"**：预设不看这张照片的直方图，它带着自己的一套看法来。这不是谁更好的问题，而是"这款胶片就这样看世界"和"为这张照片量身定制"之间的取舍。

三个事实：

- **用了曲线预设，"黑白点依据"就不起作用了。**预设自带黑白点，这时切到"传感器实测范围"不会报错，画面也完全一样。写在这里，免得你对着不变的画面困惑。（实现细节见[架构文档](ARCHITECTURE.zh-CN.md)。）
- **暗部收黑和高光收白仍然有效。**它们在预设的曲线上继续微调（黑以预设的相纸黑为底）。本节后台样张实测：Velvia 曲线上暗部收黑 −2 仍有明显效果（约一半的像素有看得出的变化），微调的余地是真实存在的。
- **同一个微调有多大余地，取决于那一段曲线上有没有内容。**Velvia 的高光是所有预设里最硬的：高光有内容的照片（本节的后台）上，高光收白 +2 还有约 7% 的像素出现看得出的变化；高光很少的照片（公园样张）只剩 0.5%，几乎纹丝不动。数字不动是曲线本身决定的，和《修图工作流说明书》里"样张一的高光收白纹丝不动"是一回事。

---

## 五 · AgX 色彩浓淡：成像这一头的浓淡调节

先说原理：这个下拉框改的是颜色变亮时**怎样过渡到白**，管的是饱和色的"浓"。（《修图工作流说明书》附录的"影调映射"一节讲过，AgX 会让很亮的颜色逐渐变淡、过渡到白；这里调的就是这个过程走得快还是慢。）它在曲线之后，属于"怎么成像"那一半。每款胶片配一个（Velvia 配浓郁，Kodachrome 配柔和），这种搭配是个人口味，随时可以改。四个选项在命令行里叫 base / punchy / smooth / muted，界面上是标准 / 浓郁 / 平滑 / 柔和。

香港街景（不用胶片，单独看这一层），双层巴士的裁切：

| 标准 base · darktable 默认 | 浓郁 punchy |
|---|---|
| ![](assets/film-tutorial/crop_primaries_base.jpg) | ![](assets/film-tutorial/crop_primaries_punchy.jpg) |

| 平滑 smooth | 柔和 muted |
|---|---|
| ![](assets/film-tutorial/crop_primaries_smooth.jpg) | ![](assets/film-tutorial/crop_primaries_muted.jpg) |

看**高饱和的巴士黄**：浓郁的黄更密、更沉；柔和的黄明显变淡；平滑在中低饱和的区域几乎和标准重合，差别集中在最饱和的颜色怎样过渡到白——它是四个选项里离标准最近的。不带颜色的路面和白色路标几乎不变：这个控件只改变颜色，不碰明暗。

低饱和的画面上，四个选项彼此很接近（本文的公园和钢桥样张几乎看不出差别）。想看清这个控件，要找画面里最饱和的那块颜色。极端的情况是这样的：live house 的红色舞台光，整片又亮又饱和，"怎样过渡到白"无处可藏——

| 浓郁 punchy | 柔和 muted |
|---|---|
| ![](assets/film-tutorial/stage_primaries_punchy.jpg) | ![](assets/film-tutorial/stage_primaries_muted.jpg) |

浓郁的红密而沉，桁架和背景上的光都带着厚度；柔和把同一片红放淡了一档，亮部的红更早地让位给白。再看不受它影响的部分：白衬衫、肤色、黑暗处几乎不变，两张图的明暗几乎一样（均值相差 2/255）。这就是"只改变饱和色、不碰明暗"在极端场景下的样子。

---

## 六 · 胶片模拟方式：风格模式与冲印模式

两种方式的对照。

打个比方。**风格模式**像是拿着数码相机，只借用这款胶片的"配色和明暗"：胶片告诉你颜色该怎么分、明暗该怎么走，最后这张图仍然是按数码的方式（AgX）呈现出来的。**冲印模式**像是真的拍了一卷：光先让底片感光，底片拿去冲洗，再放到放大机下印到相纸上，每一步都按这款胶片和相纸的数据算一遍。前者稳、快、可控；后者更接近胶片本身的脾气，可调的环节也多得多。

**风格模式**（界面"胶片风格 · AgX 成像"，命令行 `observe`，默认）：胶片决定看见了什么——白平衡、颜色的区分、明暗性格——最终的颜色由 AgX 成像。这是验证得最充分的路径，HDR 照常可用。

**冲印模式**（界面"完整冲印 · 胶片全流程"，命令行 `full`）：整个成像交给胶片。这是已经正式启用的流程，不再是实验开关。它不是"给 RGB 三个通道各套一条曲线"那种近似做法，而是按光谱把整个过程分步算出来：

1. **感光**：场景的颜色先通过一个 3×3 矩阵，换算成三层感光乳剂各自受到的曝光；部分胶片在此之上再加一张色度修正表。这张表只看颜色的色度，不看亮度，所以画面整体变亮或变暗时颜色关系不会走样。每款胶片用"只用矩阵"还是"矩阵加修正表"，是用没有参与拟合的样本验证之后，选误差小的那个；选中的方案和它的验证误差随数据文件一起发布，报告里打印的就是实际运行的那一个。
2. **冲洗**：各层的曝光经过各自的特性曲线，得到负片上的染料密度。胶片曝光和冲洗方式这两个控件在这一步起作用。
3. **印相和观看**：负片密度换算成相纸各层的曝光，经过印相曝光和相纸的显影曲线，再换算成最终看到的颜色。这一段用预先算好的两张三维查找表加几条一维曲线完成；换印相材料只换相纸那一侧，前面的步骤不用重算。

光源统一按 D55 处理。固定色温白平衡之后，钨丝灯和高显色 LED 场景的验证误差和 D55 在同一水平，所以**不设"光源类型"选项**——曾经出现过的 `--film-illuminant` 已经撤掉，不是还没做的功能。

两条需要说明的边界。第一，只有三个数字（RGB）不可能还原出完整的光谱，所以感光这一步一定有误差，每款胶片的实测误差都记录在数据里（验证样本上 99% 分位的误差，各款约 0.41–0.73 档；只用 3×3 矩阵时约 0.47–1.13 档）。第二，冲洗时各层乳剂之间互相抑制的化学效应（层间效应）**在数据手册里没有数据**。2026-08 起流程里加了一项按模型推算的层间效应：有上下限，不影响灰阶，每款胶片一个设定值，报告里写作"光谱底座 + modelled 层间放大"。不带这一项的纯光谱结果仍然可以单独得到：界面"层间效应"选"关闭 · 纯光谱"，命令行 `--film-interimage off`（本节末尾逐个说明）。这个选择会影响预览缓存和导出文件的标识，不会和默认渲染混在一起。

调了会发生什么（公园这一组 2026-09-17 重新渲染，含默认的介质柔化；"看得出的变化"按任一通道相差超过 8/255 计）：在公园样张上，两种方式之间约四成四的像素有看得出的差别，平均差别温和（约 4.5/255）。差别在树荫的绿里：风格模式的绿偏黄，冲印模式的绿更正、更冷一些（绿色像素的色相中位数相差约 26 度），亮度几乎不变（绿色像素均值 +2/255）。两条互相独立的路径——一条用 AgX 成像，一条按光谱直接计算——对同一款胶片给出了同一个方向的结果，可以互相印证。

| 风格模式 · Velvia 100 | 冲印模式 · Velvia 100 · 印相 fixed·默认配对·bounded 中性化 · 观感光学关·介质散射 declared |
|---|---|
| ![](assets/film-tutorial/park_velvia100.jpg) | ![](assets/film-tutorial/park_velvia100_fullmode.jpg) |

差别出现在哪种颜色里，取决于场景里有哪种颜色。树荫的绿只是温和地变暖；**晴空的蓝差别最大**。同一款 Velvia，香港山道的天空在冲印模式下明显转向深紫蓝（天空区域九成以上的像素有看得出的变化，蓝通道平均 +9 个码值，红 +9，绿降约 8）。这是按光谱计算自己得出的方向，和 Velvia 有名的深蓝天一致；风格模式的 AgX 成像则把蓝天留在更"标准"的位置。颜色这一侧没有外部的标准答案可以对照，两种方式都一样——这里展示的就是它们的差别本身：

| 风格模式 · Velvia 100 | 冲印模式 · Velvia 100 · 印相 fixed·默认配对·datasheet · 观感光学关·介质散射 declared |
|---|---|
| ![](assets/film-tutorial/fullmode_hk_observe.jpg) | ![](assets/film-tutorial/fullmode_hk_full.jpg) |

换一款负片对照：Portra 400 的冲印模式温和得多（两成多的像素有看得出的变化，集中在暗部密度和织物颜色的细微下沉）。两种方式之间的差距**因胶片而异**，反转片强硬的性格在按光谱计算的流程里表现得最充分：

| 风格模式 · Portra 400 | 冲印模式 · Portra 400 · 印相 fixed·默认配对·datasheet · 观感光学关·介质散射 declared |
|---|---|
| ![](assets/film-tutorial/fullmode_expo_observe.jpg) | ![](assets/film-tutorial/fullmode_expo_full.jpg) |

上面三组对比图表头里的 `bounded 中性化` / `datasheet` / `观感光学` / `介质散射 declared` 是渲染当时的参数名，分别对应现在的"全程中性"（technical-neutral）、"保留胶片偏色"（native）、"颗粒与光晕"和"介质柔化：按介质数据"。图没有重新渲染，表头照旧。

冲印模式独有的参数还有：印相材料和印相曝光方式、胶片曝光、成片调色、层间效应、冲洗方式、高光预压缩、颗粒与光晕（下面逐个说明），以及下一节的灰阶校色。冲印模式下导出 HDR，做法是"胶片印相 + 场景高光扩展"：照片里的普通版就是胶片印相本身，HDR 版只在参考白以上，按场景里确实测到的高光平滑地加亮。它不假装是物理上真实的胶片 HDR。

### 冲印模式下的其余控件

下面这些控件只在冲印模式出现。界面切回风格模式时它们会收起并归零，命令行里都标着"仅 full"。界面上每个控件只有一句话说明，完整的说明在这里。

**印相材料**（`--film-print-medium`）：负片印到哪种相纸或拷贝上，反转片直接看哪种幻灯。默认是"全部胶片选项一览"里的出厂搭配。下拉里只列已经准备好数据的材料，所以大多数胶片只有一项，下拉干脆不显示。目前多出第二项的只有 Portra 400（Kodak Supra Endura）和 Vision3 250D 含影院变体（Kodak 2393）。换材料只换相纸那一侧（相纸的显影曲线和观看换算），前面的步骤完全不变。

**胶片曝光**（`--film-exposure`，±2 EV）：拍这卷时相对标称感光度是过曝还是欠曝。**它不是成片的亮度**，它改的是负片落在特性曲线的哪一段：过曝的负片密度大，暗部离开了片基本身的密度，颜色的区分和反差随之变化；欠曝则沉进曲线的趾部。可调范围 ±2 EV 由数据文件规定，超出会报错。要让成片整体变亮变暗，请用曝光页的 EV，那是另一层。

**印相曝光方式**（`--film-print-timing`）有三档，决定放大机怎样应对上面的胶片曝光：

一句话先懂：同一张底片拿去印照片，放大机的曝光时间可以**不动**，可以**跟着底片的厚薄自动调**，也可以**自己手动拧**。底片过曝了就厚、透光少，印相时间不变的话照片会偏亮；暗房里的常规做法是跟着调，让照片亮度回到正常，只留下过曝带来的颜色和反差变化。

- **固定 · 默认**（fixed）：沿用按标准曝光算好的印相时间。胶片曝光改了，放大机没动——曝光多的负片密度大，用同一套放大机设置印出来就偏亮，就像暗房里不重新测光直接印。
- **随胶片曝光补偿**（retimed）：按暗房的惯例，随胶片曝光重新计算印相时间。整体亮度基本不变，变的是颜色、反差、暗部和高光。需要这款胶片带印相数据，全部 21 款负片都有；反转片没有印相这一步，只能用固定。
- **自定义 · 色头+印相曝光**（custom）：手动印相。在标准印相时间的基础上，加"印相曝光"滑杆（±2 EV）和色头 Y / M 带来的变化；这些都按相纸的曝光模型推算，不是实测。只对负片开放，并且要求灰阶校色选"保留胶片偏色"——手动印相就不能再让程序替你把灰阶校成中性。界面会自动切过去，命令行不这样配会直接报错。

**成片调色**（`--film-appearance`）：流程算出来的颜色，要不要再加一层风格。

先分清两个容易混的控件：**成片调色**管的是整张照片的色调风格（这款胶片印出来"该有的味道"）；下一节的**灰阶校色**只管一件事——灰色的东西最后还偏不偏色。

- **中性还原**（technical）：只走上面的流程，不加风格。命令行不给参数时的默认值。
- **参考印相**（reference）：再加一层这款胶片印在配套相纸上的典型色调。它是界面上的默认值，但需要这款胶片有对应的配方：目前有配方的是 Portra 400、Ektar 100、Velvia 100、Vision3 250D 四款，其余胶片在界面上这一项置灰并回到中性还原，命令行会直接报错。**调色强度**（0–3，默认 1）是这层色调的倍率：0 是完全不加（灰阶校色仍按这一档的规则执行），1 是配方的标准值，大于 1 是夸张。
- **自定义**（custom）：在参考印相之上再加三个有上下限的调节：**色彩丰富度**（−1..1）、**颜色深浅**（−1..1，让颜色变深但不改饱和度）、**灰阶偏色**（0..2，配方里灰阶偏色的强度；目前第一批配方这一项都是零，滑杆留作以后用）。三项分别取 0 / 0 / 1 就是配方原样。

**调色版本**（`--film-appearance-variant`）：**参考印相**（按印相来理解，默认），或**扫描版 extended**（按扫描或胶转磁来理解：方向相同，幅度是前者的 0.6，灰阶全程中性）。扫描版需要单独的数据，目前只有 Vision3 250D 有，其余胶片不出现这个下拉。

**层间效应**（`--film-interimage`）：上文那项按模型推算的层间效应。一句话先懂：彩色胶片有三层感光乳剂，冲洗时它们会互相"抢"显影剂，哪一层显影得多，就会压住旁边的层。结果是颜色之间的差别被拉大——红的更红，绿的更绿，这是胶片颜色显得"浓"的原因之一。**按胶片数据 · 默认**（declared）用这款胶片的设定值 β（各款在 0.32–1.05 之间）；**关闭 · 纯光谱**（off）回到只按光谱计算的结果，用于对照和排查；**自定义强度**（custom）出现一条滑杆，β 在 0–1.5 之间，0 等于关闭。这类在数学上没有问题、只关乎口味的量，直接交给你来调，工具不替你定。

**颗粒与光晕**（`--film-grain` / `--film-halation` / `--film-bloom`）：胶片在空间上的成像特点，不是颜色。四档：**关闭 · 默认**、**轻**（颗粒 0.25 · 光晕 0.20 · 柔光 0.15）、**标准**（0.50 · 0.40 · 0.30）、**自定义**（三条滑杆各 0–1）。三条滑杆各管一个物理上不同的过程，取 0 都是完全不处理。

- **颗粒**：胶片的颗粒感。随机的颗粒图案按负片上的毫米位置来定（36×24 mm 的片幅），所以预览、裁切、全尺寸导出看到的是同一卷片子上同一位置的颗粒——画面放大缩小，颗粒的纹理跟着变，统计特性不变。颗粒的强弱不是一个固定值，而是随密度变化（颗粒度曲线，记作 σ(D)），三个颜色层各查各的表。表是从柯达数据手册上的颗粒度曲线数字化来的（负片用 5207 / Vision3 250D，拷贝片用 2383），并按手册规定的 48 µm 测量孔径校准：按手册的方法去量输出，能得到手册上的数字；像素更细时颗粒看起来也相应更粗。目前各款胶片共用这两张表，各层染料之间在实际测量里的相互干扰也还没有建模，所以这一项的数据来源标注为"由数据推导"（derived），而不是"逐款实测"（measured）。颗粒先改变密度，再经过印相，所以它会影响后面的颜色和局部反差，不是贴在成片上的一层噪点。随机性只决定颗粒的排布（种子由下面的"颗粒种子"给出），不改颗粒的粗细和强度。
- **光晕（Halation）**：强光穿过乳剂，被片基反射回来，扩散之后**让乳剂再次感光**，其中感红层受到的影响最大。它发生在特性曲线之前，所以是随着高光"长"出来的、偏红的光晕，不是均匀的模糊。这一项是模型推算的。
- **柔光（Bloom）**：光线进入胶片之前产生的一层眩光，直接叠加在场景的光上。它纯属个人口味，不是材料本身的特性。
- **介质柔化**（`--film-media-scatter`）不在上面三条滑杆里，是这一块里单独的一个下拉。胶片乳剂和相纸本身会让影像略微变柔，这是材料的属性，不是风格选项。它由数据手册的 **MTF** 曲线拟合得到，总光量不变，按全分辨率计算，所以不管上面选的是关闭还是标准，它都照常生效。**按介质数据 · 默认**（declared）按预先算好的材料参数施加；**关闭**（off）只用于单独测量某一个效果，不是口味选项。下拉底下那行"profile · 颗粒:derived×2介质 · halation:modelled · 散射:derived · bloom:editorial"写的就是这四项的数据来源：由数据推导、模型推算、由数据推导、个人口味。
- **颗粒种子**（`--film-optics-seed`）：颗粒随机排布的起点，也在这一块里，是一个数字输入框。留空表示自动：载入这张 RAW 时随机取一次，这次载入期间固定不变，所以预览和导出一致；报告里会写出实际用的种子，方便复现。填一个非负整数就永远能复现。种子只决定排布，不改颗粒的粗细和强度。

**冲洗方式**（`--film-development`）：负片"怎么冲"。各款胶片的特性曲线是按数据手册上的标准冲洗条件测出来的，这个下拉决定要不要在它的基础上改动，有两档：

- **标准冲洗**（measured_default，默认）：按数据手册原样，下面三项锁定为 0。命令行里给了 `--film-dev-*` 却没有选自定义冲洗，会直接报错，不会悄悄生效。
- **自定义冲洗**（editorial_custom）：解锁三条有上下限的滑杆。它们都以中灰为支点，所以中灰的位置不会变，变的是曲线的形状和染料的多少；报告里会注明用了自定义冲洗。**冲洗反差**（`--film-dev-contrast`，[−0.5, 0.5]）以中灰为支点改变特性曲线的陡峭程度，相当于迫冲或减冲带来的反差变化；**灰雾**（`--film-dev-fog`，[0, 0.3]）让三层乳剂均匀地多出一层密度，也就是化学灰雾，它会让中灰和印出来的画面整体变亮，这是灰雾真实的表现，工具不会暗中补偿回去，所以它只有正方向；**染料浓度**（`--film-dev-density`，[−0.5, 0.5]）以中灰为支点改变冲洗出来的染料量。

两条限制（界面会自动处理并在状态栏说明，命令行和服务端不满足会直接报错）。自定义冲洗**要求灰阶校色选"保留胶片偏色"**（native）：全程中性和只校中灰用的校正曲线都是按标准冲洗算的，负片的密度被你改过之后它们就不成立了，界面会自动切过去，并把其它校色选项置灰。自定义冲洗也**不能和"随胶片曝光补偿"一起用**：那张补偿表同样是按标准冲洗算的，界面会把它置灰并回到固定（自定义印相曝光仍然可用）。切回标准冲洗时三项恢复为 0，切回风格模式时整块收起。

**高光预压缩**（`--film-compression`，[0, 1]，默认 0）：一句话先懂：数码传感器的高光是"硬"的，亮到上限就一刀切；胶片的高光是"软"的，越亮增长越慢。把数码的画面直接"拍"到胶片上，特别亮的部分会超出胶片能容纳的范围。这个滑杆就是在那之前先把太亮的部分压一压。具体来说：在光线"进入胶片"之前，先对场景的亮度做一次压缩，在数码传感器生硬的高光和负片的宽容度之间搭一座桥。这是个人口味项。压缩起点以上的场景亮度被平滑地压低，起点以下不变，0 是完全不处理。它作用在场景的光上、特性曲线之前，所以被压下来的高光是以正常的曝光走完后面整个流程的，不是在成片上压亮度。滑杆大于 0 时多出两条滑杆：**压缩起点**（`--film-compression-knee`，中灰之上 [0, 6] EV，默认 2）是从中灰以上第几档开始压；**高光褪色**（`--film-highlight-density`，[0, 2]，默认 0）让被压缩的高光在亮度不变的前提下褪向中性，压得越深褪得越多（C′ = C·exp(−ρd)），模拟负片高光处染料用尽、颜色变淡的样子。它只在预压缩大于 0 时有意义；预压缩归零时界面会把它复位，命令行会报错。

---

## 七 · 灰阶校色：胶片偏色，以及阴影里的交叉偏色（只在冲印模式）

界面上叫"灰阶校色"（`--film-neutralization`；旧名 `--film-crossover` 和"层间漂移"仍可用，但已弃用）。它对应的胶片术语是 crossover，中文一般叫交叉偏色。

想象你拍了一条从黑到白的灰阶条。数码相机拍出来，每一格都是不偏色的灰；胶片拍出来，往往中间几格是灰的，最暗的几格却带一点绿、一点琥珀或者一点蓝——这就是交叉偏色。这个选项问的是：**这点偏色要不要校掉？**全部校掉（全程中性）、只保证中间那一格是灰的而两头随它去（只校中灰）、完全不校（保留胶片偏色），或者让工具按你选的成片调色自己决定（默认）。

原理是这样的：真实胶片的三层感光乳剂，特性曲线并不完全平行：同样的中性灰，在暗部和亮部会偏向不同的颜色。各款胶片偏色的幅度和方向都来自数据手册上的特性曲线：Kodachrome 的阴影偏暖（琥珀色，三层乳剂在低曝光一端的密度差造成的，摄影史上有名的性格），Velvia 的阴影轻微偏冷，电影负片偏绿青。这个选项决定怎样使用这些实测数据，界面上有四个值：

- **跟随成片调色 · 默认**（界面的出厂默认；命令行不给 `--film-neutralization` 也是这个）：由第六节的"成片调色"决定——中性还原对应全程中性，参考印相对应只校中灰，扫描版 extended 对应全程中性（配方自己指定）。
- **全程中性**（technical-neutral；命令行旧名 bounded 仍可用但已弃用，内部旧名 off——下面的样张表头沿用 off）：每个通道除以这款材料在该像素亮度上的偏色（和色头用的是同一种单轴约定），每个通道的校正量限制在 ±2 档以内。材料本身灰阶偏离中性在两档以内的偏色被完全校掉；更深的偏色（典型的是 Kodachrome 最暗处的颜色）保留下来，算作材料的性格。这是一种数字上的约定，不是第二个物理过程。
- **只校中灰**（print-balanced）：同一张偏色表只在中灰这一点取一次值，算出一组固定的校正量。中灰不偏色，灰阶的两头保留材料本身随曝光变化的交叉偏色，也就是印相的性格。
- **保留胶片偏色**（native；命令行旧名 datasheet 仍可用但已弃用——下面的样张表头沿用 datasheet）：流程算出来是什么就是什么。中灰在印相那一步已经定在中性，暗部和亮部按流程自身的数据偏移。偏移的幅度目前还没有外部数据可以核对。

一条关联：印相曝光方式选"自定义 · 色头+印相曝光"时，这个选项必须是"保留胶片偏色"。手动印相和程序自动校灰阶不能同时用，界面会自动切过去，命令行不这样配会直接报错（第六节）。另外，已弃用的 `--film-crossover` 和 `--film-neutralization` 同时给出也会直接报错，新旧两个参数不要一起用。

不同的胶片，偏色的方向和幅度都不同。下面两组样张，各用偏色性格最合适的胶片。

**电影负片的绿青色阴影**：园林庭院 + Verita 200D 冲印模式（印相流程里的交叉偏色，是本文各款里幅度最大的一类），off 对比 datasheet：

| off · 中性灰保持中性 | datasheet · 按实测偏移 |
|---|---|
| ![](assets/film-tutorial/crossover_verita_off.jpg) | ![](assets/film-tutorial/crossover_verita_datasheet.jpg) |

阴影的裁切（雕花木门、石阶和卵石地）：

| off | datasheet |
|---|---|
| ![](assets/film-tutorial/crop_crossover_verita_off.jpg) | ![](assets/film-tutorial/crop_crossover_verita_datasheet.jpg) |

实测（2026-08-28 按当时的处理流程重新渲染）：有看得出变化的像素约 8%，其中八成在暗部（标准放宽到 3/255，则约七成五的像素都有轻微移动）。暗部的平均变化集中在红和绿（红 −6.5，绿 +1.8），亮部的平均值只动两三个码值，全图亮度差的中位数为 0。看什么：雕花木门和石阶平台从中性的黑转出了绿青色，白墙和受光的卵石地纹丝不动——门里门外正好是"阴影变、亮部不变"的现成分区。

**Kodachrome 的琥珀色阴影**：香港街景 + Kodachrome 64 冲印模式。这款的偏色幅度本来就温和（数据手册如此），放在这里作为"方向不同"的对照：

| off | datasheet |
|---|---|
| ![](assets/film-tutorial/crop_crossover_k64_off.jpg) | ![](assets/film-tutorial/crop_crossover_k64_datasheet.jpg) |

实测（2026-08-28 按当时的处理流程重新渲染）：有看得出变化的像素约两成三（标准放宽到 3/255 则约四成），暗部的平均值向暖移动约 16 个码值（红 +6，蓝 −10——标定去掉了观看环境的杂散光之后，数据手册上的琥珀色偏移不再被杂散光冲淡），亮部的平均值接近零。看什么：树影里的沥青路面和背光的挡土墙转出了琥珀色的暖意，日照下的路面、大巴和天空不变。（全图对比见 [off](assets/film-tutorial/crossover_k64_off.jpg) / [datasheet](assets/film-tutorial/crossover_k64_datasheet.jpg)。）

两个需要说明的边界。第一，交叉偏色出现在阴影里，**内容集中在中灰以上的明快照片几乎看不出这个选项的作用**。第二，各款胶片的幅度差别很大：电影负片最明显，Kodachrome 温和，Velvia 更温和，接近看不出来。它是一个"用不用实测偏色数据"的选择，不是风格开关。另外它只在冲印模式有意义：风格模式下选哪个都完全一样。

---

## 八 · 放大机色头：负片印相时的颜色决定（只对负片）

先说原理。彩色负片没有"自己的颜色"：底片只是中间记录，最终的颜色在放大机下由人决定，色头上黄（Y）、品红（M）两个滤色片拨到多少，就是那次决定。dngscan 的两条滑杆用的是**真实的暗房单位**：CC 滤色片密度 0–200，每格 5；30CC 等于 0.30 的光学密度，约等于让那个颜色的印相曝光减少 1 档。方向按暗房口诀：**成片偏什么色，就加什么色的滤色片**——偏黄加 Y（黄滤色片吸收蓝光，相纸的感蓝层少曝光，生成的黄染料就少），偏品红加 M。它模拟的是真实的印相光路，改档位之后亮度自动保持不变。（实现方式见[架构文档](ARCHITECTURE.zh-CN.md)"放大机色头"一节。）

两种胶片模拟方式下都有色头，只是入口不同。风格模式下，选中任何负片的曲线预设就能用——本节样张全部是风格模式。冲印模式下，固定和随曝光补偿这两种印相曝光方式的参数是一起算好的，色头要把印相曝光方式切到"自定义 · 色头+印相曝光"之后才能用（它带来的变化按相纸的曝光模型推算，不是实测；界面会同时把灰阶校色切到"保留胶片偏色"，见第六节）。其余情况下两条滑杆置灰并归零，原因写在滑杆旁边。

### 实际用多少：微调在 2–10CC

先看日常会用到的范围。真实暗房里微调的步子是 **2–10CC**，30CC 以上属于"这张片子颜色严重不对"时的大幅修正。（滑条默认的量程 0–40 CC 就是这个微调区；硬件拨轮的 200 CC 全程要勾选"色头量程扩展到 200 CC"才展开，那只是为了复刻拨轮的物理行程，不是建议你用。）清汤面碗 + Portra 400：木桌上的暖光让预设的中性印相带了一点暖，青花白瓷就是现成的中性参照物。用 Y 档小步修正：

| Y 0 · 中性印相 | Y 5 | Y 10 |
|---|---|---|
| ![](assets/film-tutorial/head_bowl_y00.jpg) | ![](assets/film-tutorial/head_bowl_y05.jpg) | ![](assets/film-tutorial/head_bowl_y10.jpg) |

调了会发生什么（实测）：Y +5CC 明显减轻了偏暖，白瓷还略微偏暖；Y +10CC 刚好到中性，青花瓷的白干净了。**这两步就是色头日常的用法**：每次 5CC 地试，看白色的东西（白瓷、白墙、白衬衫）落在中性的哪一边。

M 档同样的量反应更猛（相纸对绿光的反应更陡）：M +10CC 就把画面明显推向绿。品红—绿这个方向，微调的步子要比黄—蓝再小一半才稳。

| M 10（对照） |
|---|
| ![](assets/film-tutorial/head_bowl_m10.jpg) |

色头改的是各个颜色之间的印相平衡，不是曝光。换档时中灰的印相曝光会自动补偿。它模拟的是印相的光路，不是在成片上叠一层偏色。（档位大的时候，画面的观感自然会跟着颜色变，浓的颜色看起来更沉，那是颜色本身的分量。）两档都在 0 时和不用色头完全一样：0 就是预设自带的中性印相。

### 量程演示：30CC 起是大幅修正

再往上是为了讲清方向而做的大动作：30CC 等于 0.30 的光学密度，约等于那个颜色 **1 整档**的印相曝光，经过相纸的放大，就是剧烈的颜色变化。这不是模型夸张——同样的档位在真的暗房里同样剧烈，暗房口诀"偏什么色加什么滤色片"说的正是这种纠错的幅度：

| Y 30 | Y 60 | M 30 |
|---|---|---|
| ![](assets/film-tutorial/head_bowl_y30.jpg) | ![](assets/film-tutorial/head_bowl_y60.jpg) | ![](assets/film-tutorial/head_bowl_m30.jpg) |

实测：Y +30CC 把画面推到明显的蓝调（+60CC 更极端）；M +30CC 推到浓绿。方向和暗房口诀一致，幅度和印相的物理一致——**离谱的不是模型，30CC 本身就是暗房里的大动作**。

### 为什么反转片没有色头

E-6 / K-14 工艺的反转片冲洗出来**就是成片**：幻灯片直接投影，或者放在看片台上直接看，物理上没有印相这一步，也就没有色头这个需要做决定的环节。所以选中反转片预设时，这两条滑杆置灰归零，屏幕上写明原因（命令行传入会直接报错）。这是有意为之：只提供物理上真实存在的控制。目前各预设的分类如下（由每个预设的数据文件给出，不是按名字猜的）：

- **负片（有色头）**：Portra 160 / 400 / 800（含 push1 / push2）、Ektar 100、Gold 200、UltraMax 400、Superia 400、C200、Pro 400H、Vision3 50D / 250D / 200T / 500T、Verita 200D（含全部"·影院放映外观"变体）；
- **反转片（无色头）**：Provia 100F、Velvia 100、Ektachrome 100、Kodachrome 64。

---

## 九 · 能调到什么程度：同一张照片的六种处理

前八节把每一层控件拆开来讲，这一节把它们合起来。每张合成图有六个版本——没有哪个是"正确答案"，每个都是一种说得通的处理；每一层调整都写得出参数、可以组合、可以复现（编号对应下方的参数）。

同一张香港山道，同一款 Velvia 100：

![](assets/film-tutorial/look_grid_hk.jpg)

1. 默认参数（不用胶片）
2. `--film velvia100`
3. ② + `--scene-transform-strength 2.2 --ev -0.3`
4. ③ + `--toe-end-offset -1 --midtone-contrast 0.3`
5. `--film velvia100 --film-mode full --film-neutralization native --ev -0.3`（颜色这一侧没有外部标准答案这一点照旧；导出 HDR 时同样成立——冲印模式以"胶片印相 + 场景高光扩展"的方式参与，见第六节）
6. `--film velvia100 --grade look:optic_warm_cyan --grade-strength 0.5 --scene-transform-strength 2.0 --ev -0.2`

② 是忠实的底子 → ③④ 密度和反差逐级加强 → ⑤ 晴空转向深紫蓝 → ⑥ 在胶片之上再叠一层独立的色彩风格。

公园样张换一款负片，Portra 400——⑤ 展示负片独有的色头：

![](assets/film-tutorial/look_grid_park.jpg)

1. 默认参数（不用胶片）
2. `--film portra400`
3. ② + `--scene-transform-strength 1.8 --ev -0.2 --midtone-contrast 0.2`
4. ③ + `--toe-end-offset -1`
5. `--film portra400 --color-head-y 15`
6. `--film portra400 --film-mode full --film-neutralization native --ev -0.2`

负片的处理方式更温和：③④ 密度小步加强，⑤ 在印相这一头用 Y 档把画面往蓝的一侧修，⑥ 交给冲印模式之后绿色分得更开。

暗场景同样成立——live house 后台，Velvia 100：

![](assets/film-tutorial/look_grid_backstage.jpg)

1. 默认参数（不用胶片）
2. `--film velvia100`
3. ② + `--scene-transform-strength 2.2 --ev -0.3`
4. ③ + `--toe-end-offset -1 --midtone-contrast 0.3`
5. `--film velvia100 --film-mode full --film-neutralization native --ev -0.3`
6. `--film velvia100 --grade look:optic_warm_cyan --grade-strength 0.5 --scene-transform-strength 2.0 --ev -0.2`

品红和暗部逐级变浓，暗场景没有塌掉——同一套做法明暗场景都适用。

从"忠实的测量"到"离原片最远"是一条连续的路，停在哪里由你决定。

---

## 速查表：调了会发生什么

| 控件 | 调了会发生什么 |
|---|---|
| 选一款胶片（胶片型号） | 五层设置同时设好（白平衡 / 胶片感色 / 感色强度 / 曲线 / 色彩浓淡），全部看得见、改得了；选"无"回到纯 AgX |
| 白平衡固定色温 | 去掉相机对场景的自动适应，整卷统一用标定色温；现场光的冷暖原样保留；拍摄值本来就在标定色温附近时几乎没有变化 |
| 镜前滤镜 | 装在镜头前的色温转换滤镜，作用在胶片感色之前；没有强度滑杆 |
| 感色强度 ↑ | 颜色区分的效果加浓：同一类颜色内部的色相差距拉开；这款胶片在这类内容上本来变化就小时，几乎不动 |
| 曲线预设 | 整卷固定的明暗性格替换场景自适应；此时"黑白点依据"不起作用；暗部、高光的微调在预设曲线上仍然有效 |
| AgX 色彩浓淡 | 高饱和颜色的浓淡和过渡到白的方式改变（浓郁更密，柔和更淡，平滑接近标准）；不带颜色的内容不变 |
| 胶片模拟方式 → 完整冲印（full） | 颜色交给胶片的完整流程，整体的色相和密度重新计算（颜色这一侧没有外部标准答案这一点照旧；印相材料、印相曝光方式、成片调色、层间效应、冲洗方式、高光预压缩、颗粒与光晕随之可用；HDR 走"胶片印相 + 场景高光扩展"） |
| 灰阶校色 → 保留胶片偏色 | 只在冲印模式：中灰保持白平衡，深暗部按实测数据偏冷或偏色；中间调以上几乎不变。默认"跟随成片调色"；自定义印相曝光下自动切到这一项 |
| 印相材料 | 只在冲印模式：换相纸或拷贝只换相纸那一侧；只列已有数据的材料，多数胶片只有出厂搭配 |
| 胶片曝光 ±2 EV | 只在冲印模式：负片落在特性曲线的哪一段，不是成片的亮度；固定方式下画面随之变亮变暗，随曝光补偿下亮度基本不变，颜色、反差、暗部和高光变化 |
| 印相曝光方式 | 只在冲印模式：固定 = 按标准曝光算好的放大机设置不动；随胶片曝光补偿 = 按暗房的惯例重新计算（全部负片）；自定义 = 手动印相曝光加色头（负片，需要"保留胶片偏色"） |
| 成片调色 | 只在冲印模式：中性还原 = 只走流程不加风格；参考印相 = 加一层配套相纸上的典型色调（有配方的四款）；自定义 = 参考印相加色彩丰富度、颜色深浅、灰阶偏色 |
| 调色强度 / 调色版本 | 色调这一层的倍率 0–3（0 是不加）；扫描版 extended 目前只有 Vision3 250D 有数据 |
| 层间效应 | 只在冲印模式：按胶片数据 = 这款胶片的设定值 β；关闭 = 只按光谱计算；自定义强度 β [0, 1.5]，0 等于关闭 |
| 冲洗方式 → 自定义冲洗 | 只在冲印模式：解锁冲洗反差 [−0.5, 0.5]（以中灰为支点改变特性曲线的陡峭程度）、灰雾 [0, 0.3]（三层均匀增加密度，整体变亮）、染料浓度 [−0.5, 0.5]（以中灰为支点改变染料量）；标准冲洗把三者锁定为 0；要求灰阶校色为"保留胶片偏色"（界面自动切换，其余置灰），"随胶片曝光补偿"置灰并回到固定 |
| 高光预压缩 ↑ | 只在冲印模式：光线进入胶片之前，把压缩起点以上的场景亮度平滑压低（0 是完全不处理）；大于 0 时出现压缩起点 [0, 6] EV（默认 2）和高光褪色 [0, 2]（被压缩的高光颜色变淡） |
| 颗粒与光晕 | 只在冲印模式：颗粒（按实测颗粒度曲线，48 µm 孔径校准）、光晕（强光经片基反射，让感红层再次感光）、柔光（个人口味的眩光）；介质柔化（由 MTF 拟合）默认按所选材料生效，见下一行 |
| 介质柔化 → 关闭 | 只在冲印模式：关掉所选材料的乳剂和相纸柔化——用于单独测量某一个效果，不是风格选项；默认按介质数据 |
| 颗粒种子 | 只在冲印模式：颗粒随机排布的种子；留空 = 这次载入期间固定一次（预览和导出一致），填非负整数 = 永远可复现；只改排布，不改颗粒的粗细和强度 |
| 色头 Y ↑ | 只对负片：印相时减黄、偏向蓝；微调每步 2–10CC，30CC 起属于大幅修正；亮度自动保持不变；冲印模式下需要印相曝光方式为自定义 |
| 色头 M ↑ | 只对负片：印相时减品红、偏向绿，原理和 Y 相同，反应更猛 |

## 每个控件一句话

- **选一款胶片**（胶片型号）：一次设好五层独立设置的组合预设，没有哪一层藏在里面。
- **白平衡固定色温**：整卷统一的标定色温，去掉相机逐张的自动适应。
- **镜前滤镜**：装在镜头前的色温转换滤镜，按公布的参数作用在胶片感色之前。
- **感色强度**：颜色区分效果的浓淡倍率；1.0 是校准值，推荐值是个人口味。
- **曲线预设**：这款胶片固定的明暗性格，整卷一致，不再自适应。
- **AgX 色彩浓淡**：饱和色怎样过渡到白；对风格的影响比感色强度更大。
- **胶片模拟方式**：胶片风格 · AgX 成像 = 胶片决定看见什么，AgX 负责成像（默认）；完整冲印 · 胶片全流程 = 感光乳剂 → 特性曲线 → 印相 → 相纸 → 观看，整个流程交给胶片。
- **灰阶校色**（胶片偏色 / 交叉偏色）：只在冲印模式；跟随成片调色（默认）/ 全程中性 / 只校中灰 / 保留胶片偏色 = 中灰保持白平衡，暗部和亮部按实测数据偏移。
- **印相材料**：只在冲印模式；负片印到哪种相纸，反转片直接看哪种幻灯，只列已有数据的。
- **胶片曝光**：只在冲印模式；拍摄时相对标称感光度的过曝或欠曝（±2 EV），不是成片的亮度。
- **印相曝光方式**：只在冲印模式；固定 · 默认 / 随胶片曝光补偿（全部负片）/ 自定义 · 色头+印相曝光（负片，需要"保留胶片偏色"）。
- **成片调色**：只在冲印模式；中性还原 / 参考印相（有配方的胶片，界面默认）/ 自定义（加色彩丰富度、颜色深浅、灰阶偏色）；调色强度 0–3；调色版本 参考印相 / 扫描版 extended。
- **层间效应**：只在冲印模式；按胶片数据 · 默认 / 关闭 · 纯光谱 / 自定义强度 β [0, 1.5]。
- **冲洗方式**：只在冲印模式；标准冲洗（三项锁定为 0）/ 自定义冲洗（冲洗反差 · 灰雾 · 染料浓度三项有上下限的调节；需要"保留胶片偏色"，不能和"随胶片曝光补偿"一起用）。
- **高光预压缩**：只在冲印模式；光线进入胶片之前，对压缩起点以上的场景亮度做平滑压缩，0 是完全不处理；压缩起点（中灰以上的 EV，默认 2）和高光褪色只在预压缩大于 0 时出现。
- **颗粒与光晕**：只在冲印模式；关闭 / 轻 / 标准 / 自定义（颗粒 · 光晕 · 柔光）；这一块里另有介质柔化和颗粒种子。
- **介质柔化**：只在冲印模式；所选材料的乳剂和相纸带来的轻微柔化，按介质数据 · 默认 / 关闭（用于单独测量某一个效果）。
- **颗粒种子**：只在冲印模式；留空 = 这次载入期间固定的种子（预览和导出一致），填非负整数 = 永远可复现；只决定颗粒的排布。
- **色头 Y / M**：负片印相时两个滤色片的档位，真实的 CC 单位（0–200，每格 5；界面滑条默认 0–40，勾选后展开到 200），亮度自动保持不变；冲印模式下需要印相曝光方式为自定义。

本文全部对比图由 dngscan 按文中所列参数实际渲染：全尺寸解码；全图长边 1500–1600px，裁切是从全尺寸渲染的同一位置截取后缩放的；JPEG 质量 95。2026-08-28 起全部由 `tools/regen_showcases.py` 按 `tools/showcase_manifests/` 里写明的参数，在同一版本的处理流程下重新渲染。文中"看得出的变化"指重采样到 1200×800 之后任一通道相差超过 8/255（个别标注 3/255 的地方按标注）。全部数字来自对这些渲染结果和内部状态的直接测量，没有任何后期修饰。

样张出处：白松鼠公园 `DSC00225.ARW`（Sony A7 V）、大逆光钢桥 `DSCF0214.RAF`（X100VI）、coser 合照 `_SDI0238.DNG`（SIGMA fp）、猫吧黑猫 `_SDI0016.DNG`（SIGMA fp）、live house 红光舞台 `_SDI0175.DNG`（SIGMA fp）、品红后台 `_SDI0173.DNG`（SIGMA fp）、清汤面碗 `_SDI0127.DNG`（SIGMA fp）、香港街景 `_SDI0165.DNG`（SIGMA fp）、香港山道 `_SDI0164.DNG`（SIGMA fp）、博物馆正殿 `_SDI0094.DNG`（SIGMA fp）、Expo 周边摊 `_SDI0231.DNG`（SIGMA fp）、园林庭院 `_SDI0115.DNG`（SIGMA fp）。

# Optional dependencies for tools/calibrate_skin_matrix.py.
# Runtime export and the GUI do not need these.
# Pinned to the version the Stage A chroma-field records and pyproject declare.
colour-science==0.4.7
